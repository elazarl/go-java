package org.golang.runtime;

/**
 * Some builtin functions in go (should be statically imported).
 */
public class Builtins {
    static public void print(Object... objects) {
        for (Object object : objects) {

        }
    }
}
